/** Injetado pelo `define` do vite.config.ts: o commit na Vercel, o horário do
 *  start no dev. Serve para quebrar cache de arquivo de public/ referenciado
 *  por URL fixa. */
declare const __BUILD_ID__: string
